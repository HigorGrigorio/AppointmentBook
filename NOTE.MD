Use <%= var %> to scape all caracters, including the html code.

``` JavaScript
<=% title %>
```

Use <-% var %> to scape all caracters.

``` JavaScript
<-% title %>
```
    
use <% code %> to scape javascript code

``` JavaScript
<% for(let i = 0; i < 10; i++) { console.log(i); } %>
```